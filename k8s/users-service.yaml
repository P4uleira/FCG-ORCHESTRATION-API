apiVersion: v1
kind: Service
metadata:
  name: users-service
spec:
  type: ClusterIP
  selector:
    app: users-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP