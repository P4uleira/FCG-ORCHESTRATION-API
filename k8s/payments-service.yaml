apiVersion: v1
kind: Service
metadata:
  name: payments-service
spec:
  type: ClusterIP
  selector:
    app: payments-api
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP