apiVersion: v1
kind: Service
metadata:
  name: notifications-service
spec:
  type: ClusterIP
  selector:
    app: notifications-api
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP